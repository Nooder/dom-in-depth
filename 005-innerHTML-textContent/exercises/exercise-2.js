/*
    1. Create an HTML document that has the following
       Element in it:
       - <head> tag with a <title> in it with some text
         as well as a <script> tag
       - <body> tag with an <h1> tag that says "Welcome"

    2. Inside the <script> tag use a setTimeout with a time
       of 3000 ms to replace the entire <body> content with:
         - A <nav> tag with the following inside it:
         - a <ul> element with:
            - 3 <li> tags with text: (Home, Shop and About)

    3. Open the file in your Browser and watch the html
       change to the new content after 3 seconds

    4. Refresh the page to test it works
*/
