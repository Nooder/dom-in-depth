/*
    1. Create an HTML file with a <head> with a
       <title> with some text. Add a <body> tag
       with an <h1> tag inside with some text.

    2. Open the file in your Browser and open up the
       dev tools alongside it to run some code

    3. What are two ways you could change the text
       have white text on a black background?

    4. Make sure to test these in the console and
       confirm the results visually and in the DOM
*/
