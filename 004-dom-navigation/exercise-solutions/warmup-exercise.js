/*
    1. Create an HTML document that has at least
       one of each of the following nodes/elements:
       - head with a title that has some text inside
       - body
       - h1 with some text
       - div with a p tag inside it with some text
       - comment with some text inside
       - p with a span tag inside it, each with some text

    2. Open up the file in your Browser and open the
       developer tools

    3. How would you select (WITHOUT querySelector):
       - The title tag
       - The Comment tag
       - Any text Node
       - The p tag
       - The span tag
*/
