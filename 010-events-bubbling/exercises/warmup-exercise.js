/*
    1. Create an HTML file with a <head> with a
       <title> in it with some text, as well as
       a <body> tag
       
    2. In the body, add a <ul> Element with
       3 <li> element's inside with some different
       text for each one.

    3. How would you use event(s)/listener(s) to
       log out the text of the <li> that was clicked?

    4. Open the file in your Browser and open up
       the dev tools to verify the log when you
       click the list item
*/
